import { RootPluginReleases } from './root-plugin-releases';

describe('RootPluginReleases', () => {
  it('should create an instance', () => {
    expect(new RootPluginReleases()).toBeTruthy();
  });
});
